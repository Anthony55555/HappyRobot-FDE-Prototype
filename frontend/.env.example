# Backend API base URL for Live Mode (no trailing slash).
# Leave unset to use same origin; set to your FastAPI server when running dashboard separately.
# Example: VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
